# Base URL of the Meridian tools FastAPI service.
# In dev, this is your local uvicorn — typically http://localhost:8000.
# In production / Docker, point it at the hosted tools API.
VITE_API_BASE_URL=http://localhost:8000
